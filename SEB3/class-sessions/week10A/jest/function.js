const myAddress = {
  streetName: "london-bridge",
  flatNumber: 12,
};

const myArray = [1, 2, 3, 4, 5, 6, 7];

const myVariable = "Hello World!";

const sum = (num1, num2) => {
  return num1 + num2;
};

module.exports = { myAddress, myArray, myVariable, sum };
